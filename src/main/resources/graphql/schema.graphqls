type Book {
    id: ID!
    title: String!
    publishedYear: Int
    author: Author!
    publisher: Publisher

}
type Author {
    id: ID!
    name: String!
}
type Publisher{
    id: ID!
    name: String!
}
type Query {
    book(id: ID!): Book
    books: [Book!]!
}